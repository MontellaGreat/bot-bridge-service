# OUTPUT_CONSTRAINT_FIX_NOTE.md

# 远端 Agent 输出约束修复归档

## 背景

在 bridge 主链路、worker 实时接取、真实 `openclaw-cli` 执行已经基本打通后，仍发现一个关键问题：

- 远端 agent 实际已经执行成功
- 但输出内容会受历史上下文污染
- 出现“引用之前测试结论”“附加解释过多”“没有按固定句子回复”等现象

这会导致：
- bridge 任务虽然技术上完成
- 但最终输出不够可控
- 影响控制台展示与自动化验证可靠性

---

## 问题本质

问题不在：
- bridge 接口
- worker claim / status 流转
- executionMode 标记
- Gateway 可用性

而在：

> 远端 worker 传给 `openclaw agent --agent ... --message ...` 的 prompt 约束不够强，导致 agent 会把旧上下文一起带进回答中。

---

## 修复方向

修复策略不是继续改 bridge 状态机，而是：

### 对 worker 执行 prompt 增加强约束前缀
核心要求：
- 只依据当前任务内容作答
- 不引用任何历史对话或旧测试结论
- 如果任务要求固定回复，就只输出固定句子
- 不附加解释
- 不复盘历史
- 不推测之前状态

一句话：

> 把“执行 prompt”从普通任务转发，升级为“带严格输出约束的执行模板”。

---

## 修复结果

经远端复测确认：

- worker prompt 模板已更新
- 远端 agent 输出不再被旧上下文污染
- 固定回复任务能够精确只输出要求的句子
- 真实 CLI 执行成功
- 无 mock fallback

复测结论：

> **远端 Agent 输出约束修复完成。**
>
> bridge 协作链路不仅能执行，而且能按要求输出。

---

## 修复后的意义

这次修复补上的，不是“能不能跑”，而是“能不能可信地跑”。

修复前：
- 技术执行可能成功
- 但输出不稳定，容易混入旧上下文

修复后：
- 任务可实时接取
- 真实 CLI 可执行
- 输出可控
- 固定回复任务可用于自动验收

---

## 当前建议

后续所有用于验收、回归测试、状态确认的 bridge 任务，建议都优先采用：

- 强约束 prompt 前缀
- 固定回复句式
- 简短、明确、可自动判断的输出目标

这样可以降低：
- 上下文污染
- 输出漂移
- 自动验收误判

---

## 最终一句话

> 经过本次修复，远端 OpenClaw 已经不仅能“执行任务”，还能够“按要求稳定输出任务结果”，使整个 bridge 协作链路进入可控、可验收状态。
